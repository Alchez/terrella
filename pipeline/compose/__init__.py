"""Assemble what the site actually serves, from finished renders and from vectors that never had one.

Hero variants, border overlays and spotlights are derived from a rendered image; the country and
feature tile pyramids are cut straight from published vectors and pass through no renderer at all.
Both belong here because the step this package names is producing a delivered asset, not rendering.
"""
