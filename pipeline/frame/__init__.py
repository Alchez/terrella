"""Turn a named country into the numbers a hero render needs, and own the commands that build it.

`country_config` is the single source of truth for what rebuilds a given country, which is why the
runbook tells a reader to ask it rather than printing a command list that would drift out of date.
"""
