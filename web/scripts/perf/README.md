# Not built

This directory is empty on purpose. It was scoped as machinery for the general case of frontend
performance measurement, and nothing here was ever written.

Do not cite it as an available instrument, and do not treat the empty directory as a partial build
to finish. If a specific performance question comes up, answer that question with an instrument
sized to it; a general harness is what was already declined here.
