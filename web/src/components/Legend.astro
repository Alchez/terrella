---
// The site's hypsometric elevation legend — the shared signature element.
// Styles live in global.css (.legend / .ramp) so every page draws one key.
---

<figure class="legend" aria-label="Elevation legend, sea floor to peak">
  <div class="ramp"></div>
  <figcaption>
    <span>Sea floor</span><span>Coast</span><span>Peak</span>
  </figcaption>
</figure>
